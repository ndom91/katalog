# Migration `20200722143837-lagerify`

This migration has been generated by ndo@$(hostname) at 7/22/2020, 2:38:37 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `katalog_dev`.`Status` (
`id` int NOT NULL  AUTO_INCREMENT,`name` varchar(191) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `katalog_dev`.`Item` DROP COLUMN `status`,
ADD COLUMN `currency` varchar(191)   ,
ADD COLUMN `statusId` int  ;

ALTER TABLE `katalog_dev`.`Item` ADD FOREIGN KEY (`statusId`) REFERENCES `katalog_dev`.`Status`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200722141039-lagerify..20200722143837-lagerify
--- datamodel.dml
+++ datamodel.dml
@@ -3,18 +3,18 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model Item {
   id                   Int       @default(autoincrement()) @id
   qty                  Int?
   title                String
   description          String?
   type                 String?
-  status               String?
+  currency             String?
   purchase_price       String?
   serialNo             String?
   inventarNr           String?
   kontoNr              String?
@@ -49,8 +49,10 @@
   herkunftsart         String?
   wkn_isin             String?
   erfassungsart        String?
   images               Image[]   @relation(references: [id])
+  status               Status? @relation(fields: [statusId], references: [id])
+  statusId             Int?
   location             Location? @relation(fields: [locationId], references: [id])
   locationId           Int?
 }
@@ -63,6 +65,12 @@
 model Location {
   id          Int    @default(autoincrement()) @id
   description String
-  Item        Item[]
+  item        Item[]
 }
+
+model Status {
+  id          Int    @default(autoincrement()) @id
+  name        String
+  item        Item[]
+}
```


