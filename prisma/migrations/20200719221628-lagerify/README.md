# Migration `20200719221628-lagerify`

This migration has been generated by ndo@$(hostname) at 7/19/2020, 10:16:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `katalog_dev`.`Item` ADD COLUMN `anlag_lieferant` varchar(191)   ,
ADD COLUMN `filiale` varchar(191)   ,
ADD COLUMN `kost1` int   ,
ADD COLUMN `kost2` int   ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200718222752-lagerify..20200719221628-lagerify
--- datamodel.dml
+++ datamodel.dml
@@ -3,22 +3,22 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model Item {
-  id                   Int      @default(autoincrement()) @id
-	qty									 Int?
-	title								 String
+  id                   Int       @default(autoincrement()) @id
+  qty                  Int?
+  title                String
   description          String?
-  type          			 String?
+  type                 String?
   serialNo             String?
   inventarNr           String?
   kontoNr              String?
   date_added           DateTime
-  date_updated         DateTime @default(now())
+  date_updated         DateTime  @default(now())
   updated_by           String
   ahk_date             DateTime?
   ahk_wj_ende          String?
   buchw_wj_ende        String?
@@ -26,9 +26,13 @@
   sonder_abs_wj_ende   String?
   nutzungsdauer        String?
   afa_art              String?
   afa_percent          String?
+  kost1                Int?
+  kost2                Int?
+  filiale              String?
   lieferantNr          String?
+  anlag_lieferant      String?
   ahk_wj_beginn        String?
   buchwert_wj_beginn   String?
   n_afa_wj_beginn      String?
   sonder_abs_wj_beginn String?
@@ -42,20 +46,21 @@
   erl_afa_art          String?
   herkunftsart         String?
   wkn_isin             String?
   erfassungsart        String?
-	images							 Image[]  @relation(references: [id])
-	location						 Location? @relation(fields: [locationId], references: [id])
-	locationId					 Int?
+  images               Image[]   @relation(references: [id])
+  location             Location? @relation(fields: [locationId], references: [id])
+  locationId           Int?
 }
 model Image {
-  id                  Int      @default(autoincrement()) @id
-	url									String	
-	title								String
-	item								Item[]		 @relation(references: [id])
+  id    Int    @default(autoincrement()) @id
+  url   String
+  title String
+  item  Item[] @relation(references: [id])
 }
 model Location {
-  id                  Int      @default(autoincrement()) @id
-	description					String
+  id          Int    @default(autoincrement()) @id
+  description String
+  Item        Item[]
 }
```


